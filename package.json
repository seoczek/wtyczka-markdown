{
  "name": "wtyczka-markdown",
  "version": "0.1.0",
  "private": true,
  "description": "Rozszerzenie Chrome MV3 do konwersji zaznaczonego fragmentu strony na Markdown.",
  "type": "module",
  "scripts": {
    "audit": "npm audit --audit-level=moderate",
    "check:syntax": "node scripts/check-syntax.js",
    "lint": "node scripts/lint-runtime.js",
    "test": "vitest run",
    "verify": "npm run check:syntax && npm run lint && npm test && npm run audit"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^4.1.5"
  }
}
